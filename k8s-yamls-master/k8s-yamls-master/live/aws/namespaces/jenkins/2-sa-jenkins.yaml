---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: jenkins
  namespace: jenkins-lab
# ---
# apiVersion: rbac.authorization.k8s.io/v1
# kind: ClusterRole
# metadata:
  # name: jenkins
  # namespace: jenkins
# rules:
# - apiGroups: [""]
  # verbs: ["get", "list", "watch"]
  # resources: ["endpoints", "services", "pods"]
---
kind: ClusterRoleBinding
apiVersion: rbac.authorization.k8s.io/v1
metadata:
  name: jenkins
  namespace: jenkins-lab
subjects:
- kind: ServiceAccount
  name: jenkins # Name is case sensitive
  namespace: jenkins-lab
roleRef:
  kind: ClusterRole #this must be Role or ClusterRole
  name: medimpact:namespace-admin # this must match the name of the Role or ClusterRole you wish to bind to
  apiGroup: rbac.authorization.k8s.io
