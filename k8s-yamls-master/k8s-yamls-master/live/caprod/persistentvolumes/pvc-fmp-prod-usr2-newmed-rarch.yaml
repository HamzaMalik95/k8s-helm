# Filename pvc-<namespace>-<volume name>.yaml
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: usr2-newmed-rarch
  namespace: fmp-prod
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 300Gi
  # storageClassName: nfs
  selector:
    matchLabels:
      volname: fmp-prod.sqr-rarch