# Filename pvc-<namespace>-<volume name>.yaml
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mdrp-appdata-demo-data-mdrp
  namespace: mfs-uat
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 1000Gi
  # storageClassName: nfs
  selector:
    matchLabels:
      volname: mfs-uat.mdrp-appdata-demo-data-mdrp
