---
apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: azurechatgpt
  name: azurechatgpt
  # namespace: itsystems-dev
spec:
  replicas: 2
  revisionHistoryLimit: 2
  selector:
    matchLabels:
      app: azurechatgpt
  strategy:
    rollingUpdate:
      maxSurge: 25%
      maxUnavailable: 25%
    type: RollingUpdate
  template:
    metadata:
      labels:
        app: azurechatgpt
    spec:
      containers:
      - name: azurechatgpt
        image: docker-local.artifactory.medimpact.com/com/medimpact/azurechatgpt-restricted:0.0.1
        imagePullPolicy: Always
        env:
        - name: NEXT_TELEMETRY_DISABLED
          value: "1"
        - name: AZURE_OPENAI_API_KEY
          value: "2f3fc1592e57433b9e70a9fb14059a87"
        - name: AZURE_OPENAI_API_INSTANCE_NAME
          value: "oai-gpt-dev-useast2-001"
        - name: AZURE_OPENAI_API_DEPLOYMENT_NAME
          value: "gpt-4-32k"
        - name: AZURE_OPENAI_API_VERSION
          value: "2023-03-15-preview"
        - name: AZURE_AD_CLIENT_ID
          value: "1753d6fd-62b6-4d5e-97ad-0e5905cb6116"
        - name: AZURE_AD_CLIENT_SECRET
          value: "<REDACTED_AZURE_AD_CLIENT_SECRET>"
        - name: AZURE_AD_TENANT_ID
          value: "9c02d424-0baa-4499-b49e-a06177b472dc"
        - name: NEXTAUTH_SECRET
          value: "BzX/nng+/dsRqk1UWc8GlCHGEFYs9zxWeXNNb6RfD3E="
        - name: NEXTAUTH_URL
          value: "https://chatgpt-dev.medimpact.com"
        - name: AZURE_COSMOSDB_URI
          value: "https://cosmos-chatgpt-dev-westus2-001.documents.azure.com:443/"
        - name: AZURE_COSMOSDB_KEY
          value: "<REDACTED_AZURE_COSMOSDB_KEY>"
        ports:
        - containerPort: 3000
          name: http
          protocol: TCP
        # livenessProbe:
          # failureThreshold: 3
          # httpGet:
            # path: /healthz
            # port: 8080
            # scheme: HTTP
          # initialDelaySeconds: 30
          # periodSeconds: 10
          # successThreshold: 1
          # timeoutSeconds: 1
        # readinessProbe:
          # failureThreshold: 3
          # httpGet:
            # path: /healthz
            # port: 8080
            # scheme: HTTP
          # initialDelaySeconds: 5
          # periodSeconds: 10
          # successThreshold: 1
          # timeoutSeconds: 1
        resources:
          limits:
            cpu: "1000m"
            memory: "400Mi"
          requests:
            cpu: "0"
            memory: "300Mi"
        securityContext:
          allowPrivilegeEscalation: false
          capabilities:
            drop:
            - ALL
        # volumeMounts:
          # - name: models
            # mountPath: /llama.cpp/models
      securityContext:
        runAsUser: 10111
        runAsNonRoot: true
        seccompProfile:
          type: RuntimeDefault
      # volumes:
        # - name: models
          # persistentVolumeClaim:
            # claimName: modeltest-pvc