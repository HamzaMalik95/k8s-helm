# Filename pvc-<namespace>-<volume name>.yaml
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: usr2-newmed-out
  namespace: dmp-prod
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 275Gi
  # storageClassName: nfs
  selector:
    matchLabels:
      volname: dmp-prod.sqr-out