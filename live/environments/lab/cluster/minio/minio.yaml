# # Generated with
# helm template --namespace itsystems-lab --set rootUser='Sheiqu&eag7zohf9ohz3,rootPassword=ka0Xe9she&aGee5eba(k' --name-template=test minio/minio --set ingress.enabled=true --set consoleIngress.enabled=true > git/k8s/k8s-yamls/live/lab/minio/minio.yaml
# Modified replicas to 4
# Modified storage to 20Gi
# Modified storageClassName: itsystems-dev
# Added service.alpha.kubernetes.io/tolerate-unready-endpoints: "true" annotation to headless service
# Modified ingresses appropriately
# Commented user job
# Updated image to RELEASE.2022-06-03T01-40-53Z
---
# Source: minio/templates/serviceaccount.yaml
apiVersion: v1
kind: ServiceAccount
metadata:
  name: "minio-sa"
  namespace: "itsystems-lab"
---
# Source: minio/templates/secrets.yaml
apiVersion: v1
kind: Secret
metadata:
  name: test-minio
  namespace: "itsystems-lab"
  labels:
    app: minio
    chart: minio-4.0.2
    release: test
    heritage: Helm
type: Opaque
data:
  rootUser: "U2hlaXF1JmVhZzd6b2hmOW9oejMK"
  rootPassword: "a2EwWGU5c2hlJmFHZWU1ZWJhKGsK"
---
# Source: minio/templates/configmap.yaml
apiVersion: v1
kind: ConfigMap
metadata:
  name: test-minio
  namespace: "itsystems-lab"
  labels:
    app: minio
    chart: minio-4.0.2
    release: test
    heritage: Helm
data:
  initialize: |-
    #!/bin/sh
    set -e ; # Have script exit in the event of a failed command.
    MC_CONFIG_DIR="/etc/minio/mc/"
    MC="/usr/bin/mc --insecure --config-dir ${MC_CONFIG_DIR}"
    
    # connectToMinio
    # Use a check-sleep-check loop to wait for MinIO service to be available
    connectToMinio() {
      SCHEME=$1
      ATTEMPTS=0 ; LIMIT=29 ; # Allow 30 attempts
      set -e ; # fail if we can't read the keys.
      ACCESS=$(cat /config/rootUser) ; SECRET=$(cat /config/rootPassword) ;
      set +e ; # The connections to minio are allowed to fail.
      echo "Connecting to MinIO server: $SCHEME://$MINIO_ENDPOINT:$MINIO_PORT" ;
      MC_COMMAND="${MC} alias set myminio $SCHEME://$MINIO_ENDPOINT:$MINIO_PORT $ACCESS $SECRET" ;
      $MC_COMMAND ;
      STATUS=$? ;
      until [ $STATUS = 0 ]
      do
        ATTEMPTS=`expr $ATTEMPTS + 1` ;
        echo \"Failed attempts: $ATTEMPTS\" ;
        if [ $ATTEMPTS -gt $LIMIT ]; then
          exit 1 ;
        fi ;
        sleep 2 ; # 1 second intervals between attempts
        $MC_COMMAND ;
        STATUS=$? ;
      done ;
      set -e ; # reset `e` as active
      return 0
    }
    
    # checkBucketExists ($bucket)
    # Check if the bucket exists, by using the exit code of `mc ls`
    checkBucketExists() {
      BUCKET=$1
      CMD=$(${MC} ls myminio/$BUCKET > /dev/null 2>&1)
      return $?
    }
    
    # createBucket ($bucket, $policy, $purge)
    # Ensure bucket exists, purging if asked to
    createBucket() {
      BUCKET=$1
      POLICY=$2
      PURGE=$3
      VERSIONING=$4
    
      # Purge the bucket, if set & exists
      # Since PURGE is user input, check explicitly for `true`
      if [ $PURGE = true ]; then
        if checkBucketExists $BUCKET ; then
          echo "Purging bucket '$BUCKET'."
          set +e ; # don't exit if this fails
          ${MC} rm -r --force myminio/$BUCKET
          set -e ; # reset `e` as active
        else
          echo "Bucket '$BUCKET' does not exist, skipping purge."
        fi
      fi
    
      # Create the bucket if it does not exist
      if ! checkBucketExists $BUCKET ; then
        echo "Creating bucket '$BUCKET'"
        ${MC} mb myminio/$BUCKET
      else
        echo "Bucket '$BUCKET' already exists."
      fi
    
    
      # set versioning for bucket
      if [ ! -z $VERSIONING ] ; then
        if [ $VERSIONING = true ] ; then
            echo "Enabling versioning for '$BUCKET'"
            ${MC} version enable myminio/$BUCKET
        elif [ $VERSIONING = false ] ; then
            echo "Suspending versioning for '$BUCKET'"
            ${MC} version suspend myminio/$BUCKET
        fi
      else
          echo "Bucket '$BUCKET' versioning unchanged."
      fi
    
      # At this point, the bucket should exist, skip checking for existence
      # Set policy on the bucket
      echo "Setting policy of bucket '$BUCKET' to '$POLICY'."
      ${MC} policy set $POLICY myminio/$BUCKET
    }
    
    # Try connecting to MinIO instance
    scheme=http
    connectToMinio $scheme
    
    
    
  add-user: |-
    #!/bin/sh
    set -e ; # Have script exit in the event of a failed command.
    MC_CONFIG_DIR="/etc/minio/mc/"
    MC="/usr/bin/mc --insecure --config-dir ${MC_CONFIG_DIR}"
    
    # connectToMinio
    # Use a check-sleep-check loop to wait for MinIO service to be available
    connectToMinio() {
      SCHEME=$1
      ATTEMPTS=0 ; LIMIT=29 ; # Allow 30 attempts
      set -e ; # fail if we can't read the keys.
      ACCESS=$(cat /config/rootUser) ; SECRET=$(cat /config/rootPassword) ;
      set +e ; # The connections to minio are allowed to fail.
      echo "Connecting to MinIO server: $SCHEME://$MINIO_ENDPOINT:$MINIO_PORT" ;
      MC_COMMAND="${MC} alias set myminio $SCHEME://$MINIO_ENDPOINT:$MINIO_PORT $ACCESS $SECRET" ;
      $MC_COMMAND ;
      STATUS=$? ;
      until [ $STATUS = 0 ]
      do
        ATTEMPTS=`expr $ATTEMPTS + 1` ;
        echo \"Failed attempts: $ATTEMPTS\" ;
        if [ $ATTEMPTS -gt $LIMIT ]; then
          exit 1 ;
        fi ;
        sleep 2 ; # 1 second intervals between attempts
        $MC_COMMAND ;
        STATUS=$? ;
      done ;
      set -e ; # reset `e` as active
      return 0
    }
    
    # checkUserExists ($username)
    # Check if the user exists, by using the exit code of `mc admin user info`
    checkUserExists() {
      USER=$1
      CMD=$(${MC} admin user info myminio $USER > /dev/null 2>&1)
      return $?
    }
    
    # createUser ($username, $password, $policy)
    createUser() {
      USER=$1
      PASS=$2
      POLICY=$3
    
      # Create the user if it does not exist
      if ! checkUserExists $USER ; then
        echo "Creating user '$USER'"
        ${MC} admin user add myminio $USER $PASS
      else
        echo "User '$USER' already exists."
      fi
    
    
      # set policy for user
      if [ ! -z $POLICY -a $POLICY != " " ] ; then
          echo "Adding policy '$POLICY' for '$USER'"
          ${MC} admin policy set myminio $POLICY user=$USER
      else
          echo "User '$USER' has no policy attached."
      fi
    }
    
    # Try connecting to MinIO instance
    scheme=http
    connectToMinio $scheme
    
    
    
    # Create the users
    createUser console console123 consoleAdmin
    
  add-policy: |-
    #!/bin/sh
    set -e ; # Have script exit in the event of a failed command.
    MC_CONFIG_DIR="/etc/minio/mc/"
    MC="/usr/bin/mc --insecure --config-dir ${MC_CONFIG_DIR}"
    
    # connectToMinio
    # Use a check-sleep-check loop to wait for MinIO service to be available
    connectToMinio() {
      SCHEME=$1
      ATTEMPTS=0 ; LIMIT=29 ; # Allow 30 attempts
      set -e ; # fail if we can't read the keys.
      ACCESS=$(cat /config/rootUser) ; SECRET=$(cat /config/rootPassword) ;
      set +e ; # The connections to minio are allowed to fail.
      echo "Connecting to MinIO server: $SCHEME://$MINIO_ENDPOINT:$MINIO_PORT" ;
      MC_COMMAND="${MC} alias set myminio $SCHEME://$MINIO_ENDPOINT:$MINIO_PORT $ACCESS $SECRET" ;
      $MC_COMMAND ;
      STATUS=$? ;
      until [ $STATUS = 0 ]
      do
        ATTEMPTS=`expr $ATTEMPTS + 1` ;
        echo \"Failed attempts: $ATTEMPTS\" ;
        if [ $ATTEMPTS -gt $LIMIT ]; then
          exit 1 ;
        fi ;
        sleep 2 ; # 1 second intervals between attempts
        $MC_COMMAND ;
        STATUS=$? ;
      done ;
      set -e ; # reset `e` as active
      return 0
    }
    
    # checkPolicyExists ($policy)
    # Check if the policy exists, by using the exit code of `mc admin policy info`
    checkPolicyExists() {
      POLICY=$1
      CMD=$(${MC} admin policy info myminio $POLICY > /dev/null 2>&1)
      return $?
    }
    
    # createPolicy($name, $filename)
    createPolicy () {
      NAME=$1
      FILENAME=$2
    
      # Create the name if it does not exist
      echo "Checking policy: $NAME (in /config/$FILENAME.json)"
      if ! checkPolicyExists $NAME ; then
        echo "Creating policy '$NAME'"
      else
        echo "Policy '$NAME' already exists."
      fi
      ${MC} admin policy add myminio $NAME /config/$FILENAME.json
    
    }
    
    # Try connecting to MinIO instance
    scheme=http
    connectToMinio $scheme
    
    
  custom-command: |-
    #!/bin/sh
    set -e ; # Have script exit in the event of a failed command.
    MC_CONFIG_DIR="/etc/minio/mc/"
    MC="/usr/bin/mc --insecure --config-dir ${MC_CONFIG_DIR}"
    
    # connectToMinio
    # Use a check-sleep-check loop to wait for MinIO service to be available
    connectToMinio() {
      SCHEME=$1
      ATTEMPTS=0 ; LIMIT=29 ; # Allow 30 attempts
      set -e ; # fail if we can't read the keys.
      ACCESS=$(cat /config/rootUser) ; SECRET=$(cat /config/rootPassword) ;
      set +e ; # The connections to minio are allowed to fail.
      echo "Connecting to MinIO server: $SCHEME://$MINIO_ENDPOINT:$MINIO_PORT" ;
      MC_COMMAND="${MC} alias set myminio $SCHEME://$MINIO_ENDPOINT:$MINIO_PORT $ACCESS $SECRET" ;
      $MC_COMMAND ;
      STATUS=$? ;
      until [ $STATUS = 0 ]
      do
        ATTEMPTS=`expr $ATTEMPTS + 1` ;
        echo \"Failed attempts: $ATTEMPTS\" ;
        if [ $ATTEMPTS -gt $LIMIT ]; then
          exit 1 ;
        fi ;
        sleep 2 ; # 1 second intervals between attempts
        $MC_COMMAND ;
        STATUS=$? ;
      done ;
      set -e ; # reset `e` as active
      return 0
    }
    
    # runCommand ($@)
    # Run custom mc command
    runCommand() {
      ${MC} "$@"
      return $?
    }
    
    # Try connecting to MinIO instance
    scheme=http
    connectToMinio $scheme
---
# Source: minio/templates/console-service.yaml
apiVersion: v1
kind: Service
metadata:
  name: test-minio-console
  namespace: "itsystems-lab"
  labels:
    app: minio
    chart: minio-4.0.2
    release: test
    heritage: Helm
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 9001
      protocol: TCP
      targetPort: 9001
  selector:
    app: minio
    release: test
---
# Source: minio/templates/service.yaml
apiVersion: v1
kind: Service
metadata:
  name: test-minio
  namespace: "itsystems-lab"
  labels:
    app: minio
    chart: minio-4.0.2
    release: test
    heritage: Helm
    monitoring: "true"
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 9000
      protocol: TCP
      targetPort: 9000
  selector:
    app: minio
    release: test
---
# Source: minio/templates/statefulset.yaml
apiVersion: v1
kind: Service
metadata:
  name: test-minio-svc
  namespace: "itsystems-lab"
  labels:
    app: minio
    chart: minio-4.0.2
    release: "test"
    heritage: "Helm"
  annotations:
    service.alpha.kubernetes.io/tolerate-unready-endpoints: "true"
spec:
  publishNotReadyAddresses: true
  clusterIP: None
  ports:
    - name: http
      port: 9000
      protocol: TCP
  selector:
    app: minio
    release: test
---
# Source: minio/templates/statefulset.yaml
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: test-minio
  namespace: "itsystems-lab"
  labels:
    app: minio
    chart: minio-4.0.2
    release: test
    heritage: Helm
spec:
  updateStrategy:
    # type: OnDelete
    type: RollingUpdate
  podManagementPolicy: "Parallel"
  serviceName: test-minio-svc
  replicas: 4
  selector:
    matchLabels:
      app: minio
      release: test
  template:
    metadata:
      name: test-minio
      labels:
        app: minio
        release: test
      annotations:
        checksum/secrets: 686243008f1d10ef2c04a7321b76706d96f61aa5ba3a2b2489152f86625e0c03
        checksum/config: 6cbba9cb0f2fcb464c9d0fcb3dc0c010b0208777f4e3c5cea85d9710ff160f08
    spec:
      securityContext:
        runAsUser: 1000
        runAsGroup: 1000
        fsGroup: 1000

      serviceAccountName: minio-sa
      containers:
        - name: minio
          image: quay.io/minio/minio:RELEASE.2022-06-03T01-40-53Z
          # image: quay.io/minio/minio:RELEASE.2022-05-26T05-48-41Z
          # image: quay.io/minio/minio:RELEASE.2022-05-08T23-50-31Z
          imagePullPolicy: IfNotPresent

          command: [ "/bin/sh",
            "-ce",
            "/usr/bin/docker-entrypoint.sh minio server  http://test-minio-{0...3}.test-minio-svc.itsystems-lab.svc.cluster.local/export -S /etc/minio/certs/ --address :9000 --console-address :9001" ]
          volumeMounts:
            - name: export
              mountPath: /export            
          ports:
            - name: http
              containerPort: 9000
            - name: http-console
              containerPort: 9001
          env:
            - name: MINIO_ROOT_USER
              valueFrom:
                secretKeyRef:
                  name: test-minio
                  key: rootUser
            - name: MINIO_ROOT_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: test-minio
                  key: rootPassword
            # - name: MINIO_DISK_USAGE_CRAWL_DELAY
              # value: "0"
            # - name: MINIO_DISK_USAGE_CRAWL_DEBUG
              # value: "on"
            # - name: MINIO_PROMETHEUS_AUTH_TYPE
              # value: "public"
            - name: MINIO_PROMETHEUS_URL
              value: "http://minio-prometheus:9090"
            # OIDC works but there's issues tracking these users and serviceaccounts attached to them
            - name: MINIO_IDENTITY_OPENID_CONFIG_URL
              value: "https://sts.medimpact.com/adfs/.well-known/openid-configuration"
            - name: MINIO_IDENTITY_OPENID_CLIENT_ID
              value: "b4b4b60c-4d03-4563-bc1c-d13925bcf98b"
            - name: MINIO_IDENTITY_OPENID_CLIENT_SECRET
              value: "guC5nEr3KAOiahESpfjm9hQuYT1EMmx-UJMDoLxF"
            - name: MINIO_IDENTITY_OPENID_CLAIM_NAME
              value: "groups" # At least one AD Group will need to match a Role name in minio
            - name: MINIO_IDENTITY_OPENID_SCOPES
              value: "openid,profile"
            - name: MINIO_IDENTITY_OPENID_REDIRECT_URI
              value: "https://s3-console-lab.medimpact.com/oauth_callback"
            - name: MINIO_IDENTITY_OPENID_COMMENT
              value: "ADFS"
          resources:
            requests:
              cpu: 100m
              memory: 700Mi
            limits:
              cpu: 2000m
              memory: 2000Mi
      volumes:
        - name: minio-user
          secret:
            secretName: test-minio
  volumeClaimTemplates:
    - metadata:
        name: export
      spec:
        storageClassName: itsystems-dev
        accessModes: [ "ReadWriteOnce" ]
        resources:
          requests:
            storage: 100Gi
---
# Source: minio/templates/console-ingress.yaml
apiVersion: networking.k8s.io/v1beta1
kind: Ingress
metadata:
  name: test-minio-console
  namespace: "itsystems-lab"
  annotations:
    kubernetes.io/ingress.class: nginx
    # nginx.ingress.kubernetes.io/ssl-redirect: "false"
    nginx.ingress.kubernetes.io/force-ssl-redirect: "true"
    nginx.ingress.kubernetes.io/proxy-body-size: 1000m
    nginx.ingress.kubernetes.io/client-body-buffer-size: 10m
    # nginx.ingress.kubernetes.io/secure-backends: "true"
    # nginx.ingress.kubernetes.io/backend-protocol: "HTTPS"
    # nginx.ingress.kubernetes.io/whitelist-source-range: 0.0.0.0/0
  labels:
    app: minio
    chart: minio-4.0.2
    release: test
    heritage: Helm
spec:
  rules:
    - host: "s3-console-lab.medimpact.com"
      http:
        paths:
          - path: /
            backend:
              serviceName: test-minio-console
              servicePort: 9001
---
# Source: minio/templates/ingress.yaml
apiVersion: networking.k8s.io/v1beta1
kind: Ingress
metadata:
  name: test-minio
  namespace: "itsystems-lab"
  annotations:
    kubernetes.io/ingress.class: nginx
    nginx.ingress.kubernetes.io/ssl-redirect: "false"
    # nginx.ingress.kubernetes.io/force-ssl-redirect: "true"
    nginx.ingress.kubernetes.io/proxy-body-size: 1000m
    nginx.ingress.kubernetes.io/client-body-buffer-size: 10m
    # nginx.ingress.kubernetes.io/secure-backends: "true"
    # nginx.ingress.kubernetes.io/backend-protocol: "HTTPS"
    # nginx.ingress.kubernetes.io/whitelist-source-range: 0.0.0.0/0
  labels:
    app: minio
    chart: minio-4.0.2
    release: test
    heritage: Helm
spec:
  rules:
    - host: "s3-lab.medimpact.com"
      http:
        paths:
          - path: /
            backend:
              serviceName: test-minio
              servicePort: 9000
# ---
# # Source: minio/templates/post-install-create-user-job.yaml
# apiVersion: batch/v1
# kind: Job
# metadata:
  # name: test-minio-make-user-job
  # namespace: "itsystems-lab"
  # labels:
    # app: minio-make-user-job
    # chart: minio-4.0.2
    # release: test
    # heritage: Helm
  # annotations:
    # "helm.sh/hook": post-install,post-upgrade
    # "helm.sh/hook-delete-policy": hook-succeeded,before-hook-creation
# spec:
  # template:
    # metadata:
      # labels:
        # app: minio-job
        # release: test
    # spec:
      # restartPolicy: OnFailure      
      # volumes:
        # - name: minio-configuration
          # projected:
            # sources:
            # - configMap:
                # name: test-minio
            # - secret:
                # name: test-minio
      # containers:
      # - name: minio-mc
        # image: "quay.io/minio/mc:RELEASE.2022-06-03T01-40-53Z"
        # imagePullPolicy: IfNotPresent
        # command: ["/bin/sh", "/config/add-user"]
        # env:
          # - name: MINIO_ENDPOINT
            # value: test-minio
          # - name: MINIO_PORT
            # value: "9000"
        # volumeMounts:
          # - name: minio-configuration
            # mountPath: /config
        # resources:
          # requests:
            # memory: 128Mi
