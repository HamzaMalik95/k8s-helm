apiVersion: v1
kind: Service
metadata:
  name: pg3-parules
  namespace: pas-dev
spec:
  type: ClusterIP
  ports:
  - name: postgres
    port: 5432
    targetPort: 5432
  selector:
    app: pg3-parules