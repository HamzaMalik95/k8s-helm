# Filename pvc-<namespace>-<volume name>.yaml
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mdrp-appdata-uat-data-mdrp
  namespace: mfs-qa
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 1000Gi
  # storageClassName: nfs
  selector:
    matchLabels:
      volname: mfs-qa.mdrp-appdata-uat-data-mdrp
