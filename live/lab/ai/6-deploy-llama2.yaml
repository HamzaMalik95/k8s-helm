---
apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: llama2
  name: llama2
  namespace: itsystems-dev
spec:
  replicas: 1
  revisionHistoryLimit: 2
  selector:
    matchLabels:
      app: llama2
  strategy:
    rollingUpdate:
      maxSurge: 25%
      maxUnavailable: 25%
    type: RollingUpdate
  template:
    metadata:
      labels:
        app: llama2
    spec:
      containers:
      - command:
        - /bin/sh
        - -c
        - while [ true ]; do sleep 10; done
        image: drydock.medimpact.com/itsystems/llama2:v1
        imagePullPolicy: IfNotPresent
        name: llama2
        ports:
        - containerPort: 8080
          name: http
          protocol: TCP
        # livenessProbe:
          # failureThreshold: 3
          # httpGet:
            # path: /healthz
            # port: 8080
            # scheme: HTTP
          # initialDelaySeconds: 30
          # periodSeconds: 10
          # successThreshold: 1
          # timeoutSeconds: 1
        # readinessProbe:
          # failureThreshold: 3
          # httpGet:
            # path: /healthz
            # port: 8080
            # scheme: HTTP
          # initialDelaySeconds: 5
          # periodSeconds: 10
          # successThreshold: 1
          # timeoutSeconds: 1
        resources:
          limits:
            cpu: "10000m"
            memory: "15Gi"
            ephemeral-storage: "15Gi"
          requests:
            cpu: "2000m"
            memory: "12Gi"
        securityContext:
          allowPrivilegeEscalation: false
          capabilities:
            drop:
            - ALL
      securityContext:
        runAsUser: 1337
        runAsNonRoot: true
        seccompProfile:
          type: RuntimeDefault